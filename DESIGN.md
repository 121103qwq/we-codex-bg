# 设计

## 目标

让一张 Wallpaper Engine 壁纸可以同时显示在多个可见应用窗口中。默认目标是当前所有 Codex / ChatGPT 顶层窗口，也允许手动勾选 Steam、QQ、浏览器等窗口。

## 多窗口架构

UI 是多实例协调器，每个目标窗口启动一个独立 helper：

- 目标身份使用 `HWND + PID`。`HWND` 精确区分同一 Electron 进程内的多个顶层窗口，PID 用于防止窗口句柄在启动前被其他进程复用。
- 每个 helper 仍只维护一组目标窗口、壁纸窗口和样式快照，避免把底层恢复逻辑改成共享状态。
- 每个 Wallpaper Engine 窗口使用 `<宿主窗口名称前缀>-<HWND>` 的唯一名称，并启用严格名称匹配，禁止按尺寸误接管其他实例的壁纸窗口。
- 多目标启动必须指定壁纸文件；attach 模式只允许单目标，避免多个 helper 接管同一个现有 WE 窗口。

## 生命周期

- UI 启动所有 helper 后统一锁定启动参数；透明度消息广播到每个 helper。
- 停止时先向全部 helper 的 message-only 窗口发送 `WM_CLOSE`，再使用一个共享截止时间等待退出。
- 只有未正常退出的实例会被强制结束，并使用该实例的 `--hwnd`、`--pid`、`--we-window` 做定向恢复。
- `Ctrl+Alt+Shift+W` 由成功注册热键的 helper 广播 `WM_CLOSE` 给所有 helper，实现全窗口紧急还原。

## 控制范围

播放、暂停、静音、壁纸属性和 Wallpaper Engine 音量仍由 WE 的全局控制接口/音频会话完成，不是逐目标窗口控制。多窗口只隔离窗口挂载、透明度、停止和恢复生命周期。
